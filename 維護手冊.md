# 實驗室網站維護手冊

本文件整理了原本顯示在各頁面上的內部技術性文字與資料來源說明。
這些文字已從公開網頁上移除（讓網站對外觀感更簡潔專業），但內容本身仍具參考價值，
故完整保留於此，供日後維護者（負責人、助理、接手的 AI）查核資料出處與方法論之用。

> 移除方式：對應的 `data/*.js` 欄位內容已清空為空字串，畫面渲染邏輯改為「欄位為空時自動隱藏該提示框」，
> 因此頁面上不會再出現空白的黃色提示區塊。若日後想在頁面上重新顯示某段說明，
> 只要把對應欄位的值從下方複製回 `data/*.js` 即可，不需要修改任何 HTML/JS 版面邏輯。

---

## 關於實驗室（about.html）

**原顯示文字（已移除）：**
> 本頁內容為初版架構，實驗室沿革、特色與願景等段落仍待負責人補充。
> 請直接編輯本檔案 `about.html` 中「簡介」以下的文字內容。

**維護方式**：目前「簡介」段落文字取自 `data/site.js` 的 `homeIntro.zh`（與首頁共用）；
「所屬單位」「實驗室負責人簡介」分別取自 `data/site.js` 的 `institution` 與 `data/people.js` 的 `pi[0].bio_zh`。
若要補充實驗室沿革、特色、願景等段落，需直接編輯 `about.html` 在「簡介」區塊之後新增段落，
或另外設計對應的資料欄位。

---

## 研究方向（research.html，欄位：`data/research.js`）

### `note_zh`（原內容，已清空）
> 歷史上的「水域安全（Water Safety）」相關研究內容予以保留於實驗室發展歷程中，但目前不作為主要對外宣傳項目。

### `recentHighlightsNote_zh`（原內容，已清空）
> 摘要文字由 Claude 閱讀各篇論文全文後以 250 字內摘寫，供快速瀏覽，正式引用請以原文摘要為準。
> 每篇僅呈現單一媒體：有影音摘要（VGA）的論文顯示影片，其餘顯示論文中一張代表性圖表
> （來源：PI個人資料/期刊論文 各篇資料夾）。VGA 優先以 YouTube 內嵌播放（`youtubeId` 欄位），
> 尚未設定時退回播放本機 mp4 檔案。

**維護方式**：新增一篇「近期研究成果」時，摘要文字建議延續上述方法（讀論文全文、250 字內、繁體中文摘寫），
媒體優先用 YouTube 內嵌（設定 `media.youtubeId`），沒有影片時放一張代表性圖表（`media.type = "image"`）。

---

## 論文著作（publications.html，欄位：`data/publications.js` 與 `data/patents.js`）

### `publications.js` 的 `source`（原內容，已清空）
> 基礎資料來源：教師著作列表（個人教師表現.pdf），2026/08/31 匯出。8 篇期刊論文之 DOI 已於 2026/09
> 逐篇透過 CrossRef（api.crossref.org）查詢核實，標題以出版社公開資料為準（如與機構紀錄不同，
> 差異原因記錄於該筆 `titleNote` 欄位，僅供內部查考，不於頁面顯示）。
> 新增/修改論文請直接編輯本檔案，並在該筆記錄標註依據。

### `patents.js` 的 `source`（原內容，已清空）
> 資料來源：教師著作列表（個人教師表現.pdf），2026/08/31 匯出。專利不列入論文或計畫清單。

**維護方式**：新增/修改論文時，請直接編輯 `data/publications.js`，並在該筆記錄的 `doiStatus` /
`titleNote` 等欄位標註資料依據（這些技術欄位仍完整保留在資料檔中，只是不顯示於頁面）。
專利仍獨立於 `data/patents.js`，不列入論文或計畫清單。

---

## 計畫（projects.html，欄位：`data/projects.js`）

### `note_zh`（原內容，已清空）
> 資料來源：(1) 國家科學及技術委員會補助研究計畫查詢系統（https://wsts.nstc.gov.tw，
> 主持人：詹益東，查詢畫面截圖 2026/09），(2) 詹益東個人履歷 MOST CV_20250503.pdf 及
> ISU_中文履歷表_定稿_詹益東.docx。國防部補助計畫未收錄於國科會查詢系統，
> 故標記來源為履歷自陳，未經第三方公開資料庫查證。

### `studentProjectsNote_zh`（原內容，已清空）
> 資料來源：國家科學及技術委員會「大專學生研究計畫」查詢系統（wsts.nstc.gov.tw，
> 執行機關：海軍軍官學校電機系）查詢畫面，2026/09 查閱。僅收錄指導教授為「詹益東」之計畫；
> 同一查詢結果中另有 1 筆 107 年度計畫（學生：雷傑茗）指導教授為李仁軍，非本實驗室 PI，故不收錄。

### 各筆計畫的來源標註徽章（原本顯示在每張計畫卡片/每筆學生計畫上，已移除顯示）
每筆計畫在資料檔中仍有 `source` 欄位（`NSTC_VERIFIED` 或 `CV_SELF_REPORTED`），
原本頁面會依此顯示徽章文字：
- `NSTC_VERIFIED` → 「國科會查詢系統核實」
- `CV_SELF_REPORTED` → 「履歷自陳・未經第三方查證」

**維護方式**：`source` 欄位仍完整保留在 `data/projects.js` 每筆資料中，只是不再顯示於頁面。
新增計畫時仍建議填寫此欄位以利日後查核，只是畫面上不會出現對應徽章。

---

## 獎項榮譽（awards.html，欄位：`data/awards.js`）

### `note_zh`（原內容，已清空）
> 資料來源：詹益東個人履歷 MOST CV_20250503.pdf「AWARD AND HONOR」章節。
> 原文為英文條列，中文為輔助翻譯，如與正式獎狀名稱有出入請以獎狀原文為準。

---

## 聯絡我們（contact.html，欄位：`data/site.js` 的 `contact.note_zh`）

### `note_zh`（原內容，已清空）
> 資料來源：義守大學資訊工程學系教師名錄頁面（https://csie.isu.edu.tw/teachers/faculty），2026/09 查閱。

---

## 總結：資料來源速查表

| 頁面 | 主要依據文件 |
|---|---|
| PI / 聯絡資訊 | 義守大學資訊工程學系教師名錄頁面、PI 個人履歷（ISU 中文履歷表、MOST CV） |
| 論文（19 篇） | 個人教師表現.pdf 教師著作列表（2026/08/31 匯出）；8 篇期刊 DOI 已透過 CrossRef 核實 |
| 專利（7 項） | 同上教師著作列表 |
| 計畫 | 國科會補助研究計畫查詢系統 + PI 個人履歷（部分國防部計畫僅見於履歷，未經第三方查證） |
| 指導大專生研究計畫 | 國科會「大專學生研究計畫」查詢系統 |
| 獎項榮譽（24 筆） | PI 個人履歷 MOST CV 之 AWARD AND HONOR 章節 |

如需重新查核任何一筆資料，請對照上表回到原始文件（多存放於
`01_建置中實驗室網站資料/PI個人資料/`）逐筆核對。
