# 新成員資料收集與一鍵匯入說明

目標：新成員填寫一份 Google 表單 → 負責人執行一行指令 → `data/people.js` 自動更新。
不需要負責人手動編輯 JS 檔案。

---

## 一、建立 Google 表單（只需建立一次）

1. 前往 [Google 表單](https://forms.google.com) 建立新表單，標題建議：「海上視覺感知實驗室 成員資料表」。
2. 依序新增以下題目，**題目標題必須與下方完全一致**（含全形/半形符號），匯入腳本才能正確辨識：

   | 題目標題（照抄） | 建議題型 | 必填 |
   |---|---|---|
   | 中文姓名 | 簡答 | ✅ |
   | 英文姓名 | 簡答 | 建議 |
   | 身份/角色 | 選擇題（碩士生 / 博士生 / 大學專題生 / 研究助理 / 其他） | 建議 |
   | Email | 簡答（開啟「回應驗證→電子郵件」較保險） | ✅ |
   | 辦公室/位置 | 簡答 | 選填 |
   | 簡介 | 段落 | 選填 |
   | 個人網站或 Google Scholar 連結 | 簡答 | 選填 |
   | 大頭照網址 | 簡答（請先把照片上傳到雲端相簿/GitHub 取得公開圖片網址後填入，**不要用「檔案上傳」題型**，因為 Drive 連結無法直接當網頁圖片顯示） | 選填 |
   | 目前狀態 | 選擇題（在校成員 / 已畢業校友） | 建議 |

   > 若要新增/修改欄位，請同步修改 [`scripts/import-members-csv.mjs`](scripts/import-members-csv.mjs) 裡的 `HEADER_MAP` 對照表。

3. 把表單連結分享給新成員填寫（例如放在 LINE 群組公告、或加進實驗室入門手冊）。

## 二、把表單回覆連結到 Google 試算表並發布成 CSV（只需設定一次）

1. 在表單編輯畫面，切到「**回覆**」分頁 → 右上角綠色試算表圖示 → 「建立試算表」。
2. 打開這份自動建立的試算表 → 上方選單「**檔案 → 共用 → 發布到網路**」。
3. 選取要發布的工作表（預設是「表單回應 1」），格式選 **逗號分隔值 (.csv)**，按「發布」。
4. 複製產生的網址（形如 `https://docs.google.com/spreadsheets/d/e/xxxxxxxx/pub?output=csv`），把它記下來（可存進瀏覽器書籤或密碼管理工具），之後每次匯入都用同一個網址。

> 這個網址只會讀取到「已發布」的內容，且僅包含表單回覆的欄位資料，不含試算表其他機敏資訊；仍建議不要外流，因為任何持有此網址的人都能讀取所有已回覆成員的資料。

## 三、一鍵匯入（新成員填表後，隨時執行）

在 `02_網站原始碼` 資料夾底下開終端機（PowerShell），執行：

```bash
node scripts/import-members-csv.mjs "貼上你步驟二複製的CSV發布網址"
```

或者如果你習慣手動下載 CSV（Google 試算表「檔案 → 下載 → 逗號分隔值」），也可以改用本機檔案路徑：

```bash
node scripts/import-members-csv.mjs "C:\Users\CJSCOPE\Downloads\成員回覆.csv"
```

執行後畫面會顯示新增/更新/略過筆數，並自動覆寫 `data/people.js` 的 `members` / `alumni` 陣列（**不會動到 PI 資料**）。

比對規則：
- 以 **Email** 判斷是否為同一人；同一人重複送出表單只會更新資料，不會產生重複筆數。
- 「目前狀態」填「已畢業」等字樣的人，會自動歸類到畢業校友（`alumni`），並從研究成員（`members`）移除，反之亦然。

## 四、確認與發布

1. 用瀏覽器打開 `people.html` 檢查新資料顯示是否正常。
2. 確認無誤後，依照網站的部署方式更新到正式網址（例如 GitHub Pages：`git add` → `git commit` → `git push`）。

---

如果新成員的欄位比較複雜（例如需要放大頭照本機檔案而非外部網址），也可以請他們把照片檔案傳給負責人，
負責人手動存到 `assets/img/members/` 資料夾，再把檔名填入試算表對應該筆資料的「大頭照網址」欄位
（例如 `assets/img/members/wang.jpg`），下次執行匯入指令即可套用。
